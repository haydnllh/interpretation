[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "interpretation"
version = "0.1.0"
description = "Machine learning interpretability library"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    
]