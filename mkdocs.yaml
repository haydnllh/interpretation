site_name: Interpretation Docs
theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            docstring_style: numpy
            show_root_heading: true
            show_source: true

nav:
  - Home: index.md
  - API Reference:
      - ICE Explainer: api/ice.md
      - Counterfactual Explainer: api/counterfactual.md